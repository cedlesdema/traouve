{% extends 'base.html.twig' %}

{% block title %}County{% endblock %}

{% block body %}
    <h1>VILLES</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Objets perdus</th>
                <td>
                </td>
            </tr>
            <tr>
                <th>Departement</th>
                <td>{{ county.label }}</td>
            </tr>
            <tr>
                <th>Code Postale</th>
                <td>{{ county.zipcode }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('homepage') }}">retour à l'acceuil</a>

    <a href="{{ path('county_edit', {'id': county.id}) }}">edit</a>

    {{ include('county/_delete_form.html.twig') }}
{% endblock %}
